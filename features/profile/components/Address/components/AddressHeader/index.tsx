import { Title } from "@components/text";

const AddressHeader = () => {
    return (
        <header>
            <Title className="text-h2">Alamat</Title>
        </header>
    );
};

export default AddressHeader;
