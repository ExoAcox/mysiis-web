/* eslint-disable no-var */
export const API_KEY =
    (30)
        .toString(36)
        .toLowerCase()
        .split("")
        .map(function (A) {
            return String.fromCharCode(A.charCodeAt() + -39);
        })
        .join("") +
    (536).toString(36).toLowerCase() +
    (11)
        .toString(36)
        .toLowerCase()
        .split("")
        .map(function (l) {
            return String.fromCharCode(l.charCodeAt() + -13);
        })
        .join("") +
    (31409317545521).toString(36).toLowerCase() +
    (12)
        .toString(36)
        .toLowerCase()
        .split("")
        .map(function (f) {
            return String.fromCharCode(f.charCodeAt() + -13);
        })
        .join("") +
    (19).toString(36).toLowerCase() +
    (function () {
        var M = Array.prototype.slice.call(arguments),
            h = M.shift();
        return M.reverse()
            .map(function (z, F) {
                return String.fromCharCode(z - h - 42 - F);
            })
            .join("");
    })(...JSON.parse(process.env.NEXT_PUBLIC_API_KEY)) +
    (14).toString(36).toLowerCase() +
    (30)
        .toString(36)
        .toLowerCase()
        .split("")
        .map(function (R) {
            return String.fromCharCode(R.charCodeAt() + -39);
        })
        .join("") +
    (177).toString(36).toLowerCase() +
    (10)
        .toString(36)
        .toLowerCase()
        .split("")
        .map(function (O) {
            return String.fromCharCode(O.charCodeAt() + -13);
        })
        .join("") +
    (14).toString(36).toLowerCase() +
    (13)
        .toString(36)
        .toLowerCase()
        .split("")
        .map(function (m) {
            return String.fromCharCode(m.charCodeAt() + -13);
        })
        .join("") +
    (24).toString(36).toLowerCase() +
    (10)
        .toString(36)
        .toLowerCase()
        .split("")
        .map(function (E) {
            return String.fromCharCode(E.charCodeAt() + -13);
        })
        .join("") +
    (function () {
        var i = Array.prototype.slice.call(arguments),
            W = i.shift();
        return i
            .reverse()
            .map(function (V, c) {
                return String.fromCharCode(V - W - 52 - c);
            })
            .join("");
    })(5, 163);

export const MYSIIS_USERNAME =
    (13885).toString(36).toLowerCase() +
    (29)
        .toString(36)
        .toLowerCase()
        .split("")
        .map(function (B) {
            return String.fromCharCode(B.charCodeAt() + -71);
        })
        .join("") +
    (23975).toString(36).toLowerCase() +
    (function () {
        var m = Array.prototype.slice.call(arguments),
            s = m.shift();
        return m
            .reverse()
            .map(function (N, a) {
                return String.fromCharCode(N - s - 38 - a);
            })
            .join("");
    })(41, 124) +
    (22).toString(36).toLowerCase() +
    (function () {
        var R = Array.prototype.slice.call(arguments),
            j = R.shift();
        return R.reverse()
            .map(function (V, L) {
                return String.fromCharCode(V - j - 36 - L);
            })
            .join("");
    })(...JSON.parse(process.env.NEXT_PUBLIC_MYSIIS_USERNAME)) +
    (666).toString(36).toLowerCase() +
    (function () {
        var F = Array.prototype.slice.call(arguments),
            S = F.shift();
        return F.reverse()
            .map(function (R, T) {
                return String.fromCharCode(R - S - 59 - T);
            })
            .join("");
    })(47, 221);

export const MYSIIS_PASSWORD =
    (803673446841).toString(36).toLowerCase() +
    (29)
        .toString(36)
        .toLowerCase()
        .split("")
        .map(function (U) {
            return String.fromCharCode(U.charCodeAt() + -71);
        })
        .join("") +
    (342577).toString(36).toLowerCase() +
    (29)
        .toString(36)
        .toLowerCase()
        .split("")
        .map(function (d) {
            return String.fromCharCode(d.charCodeAt() + -71);
        })
        .join("") +
    (5688).toString(36).toLowerCase() +
    (function () {
        var s = Array.prototype.slice.call(arguments),
            E = s.shift();
        return s
            .reverse()
            .map(function (w, t) {
                return String.fromCharCode(w - E - 46 - t);
            })
            .join("");
    })(...JSON.parse(process.env.NEXT_PUBLIC_MYSIIS_PASSWORD)) +
    (665392).toString(36).toLowerCase() +
    (function () {
        var n = Array.prototype.slice.call(arguments),
            E = n.shift();
        return n
            .reverse()
            .map(function (z, y) {
                return String.fromCharCode(z - E - 61 - y);
            })
            .join("");
    })(53, 170);

export const TELKOM_USERNAME =
    (function () {
        var h = Array.prototype.slice.call(arguments),
            k = h.shift();
        return h
            .reverse()
            .map(function (m, q) {
                return String.fromCharCode(m - k - 0 - q);
            })
            .join("");
    })(...JSON.parse(process.env.NEXT_PUBLIC_TELKOM_USERNAME)) +
    (18920).toString(36).toLowerCase() +
    (function () {
        var A = Array.prototype.slice.call(arguments),
            h = A.shift();
        return A.reverse()
            .map(function (j, I) {
                return String.fromCharCode(j - h - 36 - I);
            })
            .join("");
    })(39, 185, 186);

export const NPS_PASSWORD =
    (22489788).toString(36).toLowerCase() +
    (function () {
        var i = Array.prototype.slice.call(arguments),
            a = i.shift();
        return i
            .reverse()
            .map(function (d, R) {
                return String.fromCharCode(d - a - 13 - R);
            })
            .join("");
    })(...JSON.parse(process.env.NEXT_PUBLIC_NPS_PASSWORD)) +
    (29)
        .toString(36)
        .toLowerCase()
        .split("")
        .map(function (N) {
            return String.fromCharCode(N.charCodeAt() + -71);
        })
        .join("") +
    (198689).toString(36).toLowerCase() +
    (29)
        .toString(36)
        .toLowerCase()
        .split("")
        .map(function (z) {
            return String.fromCharCode(z.charCodeAt() + -71);
        })
        .join("") +
    (479952).toString(36).toLowerCase() +
    (29)
        .toString(36)
        .toLowerCase()
        .split("")
        .map(function (g) {
            return String.fromCharCode(g.charCodeAt() + -71);
        })
        .join("") +
    (5424279).toString(36).toLowerCase() +
    (function () {
        var q = Array.prototype.slice.call(arguments),
            L = q.shift();
        return q
            .reverse()
            .map(function (B, S) {
                return String.fromCharCode(B - L - 37 - S);
            })
            .join("");
    })(15, 112, 107, 107, 152, 101, 106) +
    (4).toString(36).toLowerCase();

export const RPA_WIBS_PASSWORD =
    (function () {
        var o = Array.prototype.slice.call(arguments),
            j = o.shift();
        return o
            .reverse()
            .map(function (G, T) {
                return String.fromCharCode(G - j - 30 - T);
            })
            .join("");
    })(...JSON.parse(process.env.NEXT_PUBLIC_RPA_WIBS_PASSWORD)) +
    (29)
        .toString(36)
        .toLowerCase()
        .split("")
        .map(function (V) {
            return String.fromCharCode(V.charCodeAt() + -71);
        })
        .join("") +
    (669106).toString(36).toLowerCase() +
    (29)
        .toString(36)
        .toLowerCase()
        .split("")
        .map(function (i) {
            return String.fromCharCode(i.charCodeAt() + -71);
        })
        .join("") +
    (145).toString(36).toLowerCase() +
    (function () {
        var z = Array.prototype.slice.call(arguments),
            f = z.shift();
        return z
            .reverse()
            .map(function (s, I) {
                return String.fromCharCode(s - f - 51 - I);
            })
            .join("");
    })(19, 170, 124) +
    (29)
        .toString(36)
        .toLowerCase()
        .split("")
        .map(function (E) {
            return String.fromCharCode(E.charCodeAt() + -71);
        })
        .join("") +
    (420451).toString(36).toLowerCase() +
    (29)
        .toString(36)
        .toLowerCase()
        .split("")
        .map(function (T) {
            return String.fromCharCode(T.charCodeAt() + -71);
        })
        .join("") +
    (4216).toString(36).toLowerCase() +
    (function () {
        var V = Array.prototype.slice.call(arguments),
            a = V.shift();
        return V.reverse()
            .map(function (G, j) {
                return String.fromCharCode(G - a - 59 - j);
            })
            .join("");
    })(14, 136, 180, 174, 132, 177, 174, 127) +
    (6).toString(36).toLowerCase() +
    (function () {
        var K = Array.prototype.slice.call(arguments),
            B = K.shift();
        return K.reverse()
            .map(function (Z, T) {
                return String.fromCharCode(Z - B - 38 - T);
            })
            .join("");
    })(33, 125);
